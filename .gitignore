# compose reads this for ${GLANCE_GITHUB_TOKEN} etc — keep secrets out of git
.env

# Beszel runtime state — hub DB, agent keys, the hub<->agent unix socket.
# All generated on the server; nothing here is authored by us.
beszel/
.DS_Store
