# Home Assistant runtime state — secrets, database, logs, cache.
# We only version the config files we author; HA generates the rest at runtime.
config/*
!config/configuration.yaml
!config/automations.yaml
!config/scripts.yaml
!config/scenes.yaml
!config/.gitkeep

# never commit secrets
config/secrets.yaml
