server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    # The saved page is "Claim eCard.html" (with a space); serve it at /.
    index "Claim eCard.html";

    location / {
        try_files $uri $uri/ "/Claim eCard.html";
    }
}
