# Copy to .env on the server (it's gitignored). docker-compose auto-reads it.
# Optional: a GitHub fine-grained/classic token with public read scope.
# Without it, the Glance repository/releases widgets use the 60/hr anonymous
# limit — fine for the current widget count, but a token gives headroom.
GLANCE_GITHUB_TOKEN=
